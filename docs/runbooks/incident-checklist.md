# Incident Checklist

- Confirm the affected URL and timestamp.
- Check the latest GitHub Actions deployment.
- Reproduce locally with `./scripts/build.sh`.
- Inspect browser console and network failures.
- Roll back the last deployment if user impact is confirmed.
