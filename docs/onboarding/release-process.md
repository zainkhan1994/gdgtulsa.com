# Release Process

1. Make and test a focused change.
2. Run `./scripts/lint.sh` and `./scripts/test.sh`.
3. Commit to a feature branch and open a pull request.
4. Merge to `main` after review.
5. The GitHub Pages workflow builds and deploys the site from `build/`.
