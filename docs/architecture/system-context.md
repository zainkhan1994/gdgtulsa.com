# System Context

Visitors use the GitHub Pages frontend. Firebase provides member authentication and Firestore-backed data. Browser analytics events are sent to the Cloud Run collector, which writes to BigQuery. The private admin service runs separately from the public site.
